import { styled } from '@stitches/react'

export const StitcheWrapper = styled('main', {
  width: '100%',
  height: '100vh',
  display: 'flex',
  minHeight: '100vh',
  alignItems: 'center',
  flexDirection: 'column',
  justifyContent: 'center',
})
